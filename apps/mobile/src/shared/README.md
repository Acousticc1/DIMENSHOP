# Shared Module

## Purpose
Cross-cutting concerns shared across all layers.

## Structure
```
shared/
├── utils/       # Formatters, validators, logger
├── constants/   # Config, routes, feature flags
├── types/       # Shared TypeScript types
└── README.md
```
